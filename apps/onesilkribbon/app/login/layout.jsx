export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your One Silk Ribbon account to track orders, save addresses and check out faster.',
  alternates: { canonical: '/login' },
  robots: { index: false, follow: true },
}

export default function LoginLayout({ children }) {
  return children
}
