{
  "name": "osr-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "node scripts/site.js dev",
    "build": "node scripts/site.js build",
    "start": "node scripts/site.js start",
    "lint": "node scripts/site.js lint"
  }
}
